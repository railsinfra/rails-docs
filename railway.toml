[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Listens on $PORT via image CMD in Dockerfile.
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
